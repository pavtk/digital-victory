# Digital Victory

[![Python](https://img.shields.io/badge/python-3.12-blue)](https://www.python.org/)
[![Flask](https://img.shields.io/badge/flask-2.3-green)](https://flask.palletsprojects.com/)
[![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0-orange)](https://www.sqlalchemy.org/)

**Digital Victory** — это простое Flask-приложение для управления пользователями: просмотр списка, добавление нового пользователя через веб-интерфейс с Bootstrap и взаимодействие с базой данных через SQLAlchemy.

---

## Шаги по развёртыванию

### 1️⃣ Установка виртуального окружения

Создаём виртуальное окружение с Python 3.12:

```bash
python3.12 -m venv .venv
```

> Рекомендуется использовать именно `.venv`, чтобы окружение было локальным для проекта.

---

### 2️⃣ Активация виртуального окружения

* **Linux/macOS:**

```bash
source .venv/bin/activate
```

* **Windows (cmd):**

```cmd
.venv\Scripts\activate
```

* **Windows (PowerShell):**

```powershell
.venv\Scripts\Activate.ps1
```

> После активации вы увидите имя виртуального окружения в терминале, например `(.venv)`.

---

### 3️⃣ Клонируем репозиторий

```bash
git clone https://github.com/pavtk/digital-victory.git
cd digital-victory
```

---

### 4️⃣ Устанавливаем зависимости

```bash
pip install -r requirements.txt
```

> Все необходимые пакеты для работы приложения будут установлены в виртуальном окружении.

---

### 5️⃣ Создаём файл `.env`

В корне проекта создайте файл `.env` для хранения переменных окружения:

```bash
touch .env
```

---

### 6️⃣ Добавляем переменные окружения

Откройте `.env` и добавьте:

```env
FLASK_ENV=development
DATABASE_URL=sqlite:///db.sqlite3
```

> * `FLASK_ENV=development` — запуск приложения в режиме разработки.
> * `DATABASE_URL` — путь к базе данных SQLite.

---

### 7️⃣ Применяем миграции базы данных

```bash
flask db upgrade
```

> Эта команда создаст базу данных и применит все миграции.

---

### 8️⃣ Запуск приложения

```bash
flask run
```

* Приложение будет доступно по адресу: [http://127.0.0.1:5000](http://127.0.0.1:5000)
* Все фронтенд-файлы (HTML/JS/CSS) раздаются Flask на том же порту.

---

### 9️⃣ Радуемся 🎉

Если вы видите страницу с пользователями и форма добавления нового пользователя работает — значит, всё настроено правильно.

---

### 🔹 Примеры использования

* Просмотр всех пользователей через веб-интерфейс.
* Добавление нового пользователя через модальную форму.
* Доступ к API: `GET /users` и `POST /users`.

```bash
curl http://127.0.0.1:5000/users
```

---

### ⚡ Советы

* Для остановки приложения: `Ctrl+C`.
* Для повторной активации виртуального окружения: `source .venv/bin/activate` (Linux/macOS) или `.venv\Scripts\activate` (Windows).
* Если хотите поменять базу данных — достаточно изменить `DATABASE_URL` в `.env`.
* Все миграции находятся в папке `migrations/`.
* Для продакшн-развёртывания рекомендуется использовать Gunicorn и Nginx.

---

**Успехов в работе с Digital Victory! 🚀**
